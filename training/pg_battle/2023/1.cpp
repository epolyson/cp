#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

#define FAST ios::sync_with_stdio(0),cin.tie(0),cout.tie(0)

int main(){
    FAST;
    double n;
    cin >> n;
    cout << fixed << setprecision(10) << pow(0.5,n) << endl;
    return 0;
}
